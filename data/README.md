# `data/` — what every file in here is

Two layers, both committed:

```
remote sources  ->  data/raw/ (cached verbatim)  ->  data/*.csv (tidy)  ->  FINDINGS.md, docs/answers/
```

`data/raw/` is the evidence as published — never edited, never regenerated by
hand. The top-level CSVs are derived and disposable: delete any of them and the
script named below rebuilds it. To force a re-download, delete the specific file
in `data/raw/`; there are no cache-busting flags.

**One exception:** `data/raw/proposed_gtfs/` came from PRT directly and exists at
no URL. It cannot be re-fetched. Deleting it loses it.

For where each source comes from and what it is authoritative for, see
`DATA_SOURCES.md`. This file is the inventory of what is on disk.

## Tidy outputs

| File | Rows | Written by | What it is |
|---|---|---|---|
| `route_crosswalk.csv` | 107 | `ingest_blr.py` | current # → draft 2.0 # → final #, change category |
| `service_levels.csv` | 240 | `ingest_blr.py` | route × day type: span + headway in 7 periods |
| `proposed_stops.csv` | 7,036 | `ingest_blr.py` | proposed-network stop inventory (Remix) |
| `proposed_routes.csv` | 100 | `ingest_blr.py` | proposed lines with GTFS ids and colours |
| `proposed_stop_sequences.csv` | 10,464 | `ingest_blr.py` | route × direction × ordered stops |
| `current_routes.csv` | 102 | `ingest_blr.py` | current-network `routes.txt`, verbatim |
| `exhibit_a.txt` | 1,311 lines | `ingest_blr.py` | the legally noticed change narrative, as text |
| `stop_service_change.csv` | 5,782 | `analyze_service_loss.py` | per stop: keeps / loses all service, distance to nearest proposed stop |
| `stop_frequency_change.csv` | 5,747 | `analyze_frequency_change.py` | per location: weekday trips now vs proposed, by period, at 400 m and 150 m |
| `coverage_change.csv` | 5,751 | `analyze_coverage_change.py` | per location: the five coverage tiers for both networks, at 400 m and 150 m, plus trips by day type |
| `stop_route_replace.csv` | 371 | `analyze_coverage_change.py` | locations where a different route provides comparable service |
| `route_service_days.csv` | 74 | `analyze_coverage_change.py` | per route: day types now vs proposed, days lost / gained |
| `coverage_area.csv` | 10 | `analyze_coverage_area.py` | radius × tier: covered land area now vs proposed, km² lost / gained / retained |
| `coverage_area_blocks.csv` | 450 | `analyze_coverage_area.py` | every contiguous block of lost or gained coverage over 0.1 km², with place, nearest stop, centroid |
| `coverage_area_places.csv` | 452 | `analyze_coverage_area.py` | net km² gained or lost per municipality / neighbourhood, per tier |
| `coverage_area_ondemand.csv` | 10 | `analyze_coverage_area.py` | per proposed on-demand zone: area, fixed-route coverage inside it now vs proposed, lost area, vehicles, hours |
| `oneseat_change.csv` | 369 | `analyze_one_seat.py` | place × anchor: gains / keeps / loses a one-seat ride |
| `discontinued_route_ridership_202604.csv` | 20 | `analyze_route_ridership.py` | riders on each discontinued route, Apr 2026 |
| `route_frequency_change.csv` | 108 | `analyze_route_hours.py` | per corridor group: trips and in-service hours now vs proposed, per day type |

### From `ingest_blr.py` (run this first; everything else reads its output)

**`route_crosswalk.csv`** — the Find My Route HTML table. `current_route`,
`draft2_route`, `final_route`, `category` (Modified 73, Discontinued 20, New
14), `related_routes`, and the `route_page` URL for the plain-language bullets.
The join key for almost everything else. A `current_route` cell may name several
routes (`"86, 77"`); the parser splits them, a naive read loses one silently.

**`service_levels.csv`** — the three Frequency & Hours PDFs, one row per route
per day type (weekday / saturday / sunday). `start_time`–`end_time` is the
published span; `early_4_6a` … `owl_11p_4a` are headways in minutes, blank where
the route does not run in that period. `is_new` marks routes with no current
counterpart.

Since PRT's proposed GTFS arrived this is a **published cross-check, not the
primary frequency source**. Two known biases: the published span end is the last
departure from the anchor rather than from the route, running a median 96
minutes early; and span ÷ headway cannot express a midday gap, so peak-only
routes look like they run all day. `verify_proposed_gtfs.py` measures both.

**`proposed_stops.csv`** — 7,036 stops from the Remix map: `stop_uuid`,
`gtfs_stop_id`, name, lat/lon, wheelchair flag. This is an **inventory, not a
service list** — 1,416 of these stops are on no proposed route. Filter through
`proposed_stop_sequences.csv` before mapping anything.

**`proposed_stop_sequences.csv`** — 10,464 stop-visits over 200 route
directions: `route_id`, `pattern`, `direction`, `headsign`, `stop_sequence`,
`stop_uuid`, `meters_from_start`. The sequence half of `stop_times.txt` with no
times attached — Remix publishes no timetables.

**`current_routes.csv`** — `routes.txt` out of the published current GTFS,
unmodified. The before-side route list, including rail and the inclines.

**`exhibit_a.txt`** — `pdftotext -layout` of Exhibit A, the document PRT is
legally required to notice. Quote from here when the question is what PRT
formally proposed rather than what the data shows.

### Analysis outputs

**`stop_service_change.csv`** — one row per currently served stop. `status` is
`kept` (4,568), `kept_nearby` (344 — the stop id vanished but the proposal
serves a stop within 150 m) or `loses_all_service` (870), with
`metres_to_nearest_proposed_stop` showing the work. `confidence` is `confirmed`
(5,766) or `unverifiable` (16 — served today, absent from Remix, possibly added
after Remix's 2023 base feed); the unverifiable tier never enters a headline.
Boardings are carried per day type for weighting.

Proposed side is **Remix**, not the proposed GTFS — this script has not been
moved over yet, so its stop inventory is the 2023-base one.

**`stop_frequency_change.csv`** — how much service each location has, not merely
whether it has any. `current_trips` / `proposed_trips` / `pct_change` are
weekday trips within 400 m; the `_150m` columns are the strict same-corner
sensitivity. `cur_*` / `prop_*` period columns break the day into the seven
published periods. Both sides are counted from real GTFS `stop_times` by the
same code, a trip counted at the stop it calls at and at its departure time
there.

**`coverage_change.csv`** — the widest file here, and the one behind
`COVERAGE-CHANGE` and `STOP-LOST-SERVICE`. Per location it carries weekday /
Saturday / Sunday trips both ways, weekday trips by period, both route lists,
and then the five BASE_CAMP tiers as 0/1 flags for each network:
`{cur,prop}_week_any_minimum`, `_weekdays_any_minimum`, `_weekends_any_minimum`,
`_week_any_hourly`, `_weekend_any_hourly`, each repeated with a `_150m` suffix.
`hourly` means a **maximum** gap of 60 minutes anywhere in 6am–6pm on the better
direction — counting the wait from 6am to the first departure and from the last
to 6pm — not an average, which peak-only service would pass.

**`stop_route_replace.csv`** — the "different bus, same corner" case: locations
keeping comparable weekday trip counts while the routes serving them change.
`routes_lost` / `routes_gained` after translating renumbering away. These are
riders who must relearn the network without losing service.

**`route_service_days.csv`** — one row per modified route.
`current_days` / `proposed_days` are `weekday;saturday;sunday` sets;
`proposed_days_with_variants` folds in the S-variants (29S, 53S, 55S, 69S, 78S,
89S), which is where several corridors' weekend service actually went. Quote
`days_lost_corridor` / `days_gained_corridor` rather than `days_lost` /
`days_gained` — the route-wise columns read a variant as a loss it is not.
`riders_weekday/saturday/sunday` weight the change.

**`oneseat_change.csv`** — one row per place × anchor, 186 for Downtown and 183
for Oakland. `status` is `keeps` (242), `gains` (20), `loses` (27) or
`none either way` (80), with `routes_kept` / `routes_lost` / `routes_gained`
naming the routes responsible. The unit is the place — city neighbourhood, else
municipality — because per-stop the adjacent stop ids on a corridor split the
route set and manufacture losses. PRT's `HOOD`/`MUNI` labels are outlier-filtered
first; they mislabel some stops by up to 40 km.

**`discontinued_route_ridership_202604.csv`** — WPRDC average riders by day type
for each of the 20 discontinued routes, April 2026. Preferred over stop-level
sums for any route total: official, weighted, and eleven months fresher.

### The `coverage_area*.csv` set — the same tiers, measured as ground

`analyze_coverage_area.py` writes four files. They answer the half of
`COVERAGE-CHANGE` that asks for area, and they are the only files here whose
denominator is not a stop: the union of walk-radius discs around every served
stop, rasterised on a 100 m lattice.

**`coverage_area.csv`** — 10 rows, one per radius × tier. `current_km2`,
`proposed_km2`, `net_km2`, `net_pct`, `lost_km2`, `gained_km2`, `retained_km2`.
Lost and gained are different ground, so they do not net out; `net_km2` is the
smaller statement and `lost_km2` the more useful one.

**`coverage_area_blocks.csv`** — one row per contiguous block of change over
0.1 km². `place` is the commonest label among the block's cells and `also` the
runners-up, because a block is ground and ground crosses municipal lines.
`nearest_stop` is the nearest stop in whichever network *has* the service — the
stop being lost for a lost block, added for a gained one — so it names the road.

**`coverage_area_places.csv`** — net km² per municipality or neighbourhood per
tier, by nearest labelled stop within 2 km. Indicative: a cell midway between two
places goes to whichever stop is nearer, and the HOOD/MUNI labels are
outlier-filtered but still imperfect. `unplaced` is ground over 2 km from any
labelled stop, which is where genuinely new coverage shows up.

**`coverage_area_ondemand.csv`** — the 10 proposed on-demand zones from
`raw/remix_project.json`, the only part of the plan no GTFS can express.
`lost_km2_inside` is the headline: 18.3 of the 80.1 km² losing all fixed-route
service sits inside a zone. Read `vehicles_weekday` beside it — 1 to 3 vans for
the whole zone — and `hidden_in_remix`, which is 1 for all ten.

### `route_frequency_change.csv` — read the unit before quoting it

**`analyze_route_hours.py` writes this file**, from real timetables on both sides
via `gtfs.py`. It answers `LOSE/GAIN-SERVICE-HOURS`
([LOSE](../docs/answers/LOSE-SERVICE-HOURS.md),
[GAIN](../docs/answers/GAIN-SERVICE-HOURS.md)). It replaced an orphaned
route-to-route file of the same name that predated both the 8–11pm headway column
fix and the proposed GTFS; nothing PDF-derived survives in it.

One row is a **corridor group** — the connected component joining current route
numbers to the proposed numbers PRT maps them to, plus the S-variants the
crosswalk omits — never a route compared to itself. Two things to know before
quoting a row:

- **Hours are in-service time only**, first stop to last. Layover, deadhead and
  pull-in/pull-out are not in a GTFS, so this is a floor on platform hours and is
  not comparable to PRT's service-hour budget.
- **A group is not a corridor.** Where a route PRT records as NEW covers an
  incumbent's corridor, the two sit in separate groups and nothing adds them
  together — route 45 against the 51 in Carrick is the case to know. Location
  questions belong to `coverage_change.csv`, not this file.

`riders_weekday` sums WPRDC averages for the group's *current* routes, so new
groups are 0 by construction and the column is not a projection.

## `data/raw/` — cached sources

| File | Size | Source | Vintage |
|---|---|---|---|
| `proposed_gtfs/` | 66 MB, 10 files | **From PRT, at no URL** — sent to PPT on request, passed on from there | stamped 2026-08-11, calendars over 2027 |
| `current_gtfs.zip` | 22 MB | `rideprt.org/developerresources/GTFS.zip` | feed `Merged_Clever_2606_2`, valid 2026-06-28 → 2026-10-14 |
| `remix_map.json` | 9.4 MB | `platform.remix.com/api/maps/632ce361` | base feed 2023-06-18 |
| `remix_project.json` | 59 KB | `platform.remix.com/api/projects/82ea6210` | fetched 2026-08-17 |
| `stop_usage_202505.csv` | 2.5 MB | ArcGIS `PRT_Bus_Stop_Usage_Unweighted` | boardings, May 2025 |
| `route_ridership_202604.csv` | 10 KB | WPRDC CKAN SQL | through April 2026 |
| `exhibit_a.pdf` | 785 KB | `rideprt.org` | published 2026-08-17 |
| `service_weekday.pdf` / `_saturday` / `_sunday` | ~100 KB each | engage.rideprt.org S3 | published 2026-08-17 |
| `findmyroute.html` | 212 KB | `engage.rideprt.org/buslineredesign/BLR-finaldraft-findmyroute` | published 2026-08-17 |

**`proposed_gtfs/`** is the only source here that cannot be re-downloaded. Two
things about it are evidenced by the feed itself: `feed_info.txt` names
Pittsburgh Regional Transit as publisher, and its `feed_version` reads
`Updated: Aug 11, 2026, 12:14 PM` — six days before the Proposed Final Network
was published. **It reached this repo by request:** PPT asked PRT for it, PRT
sent it by email, and PPT passed it along; no date is recorded for that email
(`DATA_SOURCES.md`). Nothing on the feed proves it is the final plan
rather than a working draft either, and `verify_proposed_gtfs.py` earns that
claim against the documents PRT did publish: route set, day types and span must
agree with the Frequency & Hours PDFs (they do), while trip volume is expected
to disagree and the size of the disagreement is why the feed supersedes the PDF
model. Contents: `Future_BLR_Service-Weekday/-Sa/-Su` calendars over 2027, 5,515
stops, 14,488 trips, 698,865 `stop_times` rows.

**`stop_usage_202505.csv`** is 19,854 stop × route rows over 7,076 stops. Two
traps: take boardings from the `All Routes` row (summing the per-route rows
double-counts), and quote PRT's own disclaimer — these are "unadjusted,
unofficial totals" that may understate ridership by as much as 30%. Boardings
only; alightings stopped in September 2023.

## Rebuilding

```bash
python3 ingest_blr.py               # must run first
python3 analyze_service_loss.py
python3 analyze_route_ridership.py
python3 analyze_frequency_change.py
python3 analyze_one_seat.py
python3 analyze_coverage_change.py
python3 analyze_coverage_area.py    # optional lattice size in metres, default 100
python3 analyze_route_hours.py
python3 verify_proposed_gtfs.py     # checks the supplied feed against the PDFs
```

`analyze_one_seat.py` reads only raw sources and runs independently.
`gtfs.py` is the shared reader both networks are measured through — it is
imported, not run.

## Reading anything in here

Four conventions carry across every file; each was arrived at because the naive
version produced a wrong answer with a confident face on it. `CLAUDE.md` has the
full list.

1. **Never compare route N to route N.** The plan re-splits corridors. The unit
   is a location or a place, with the same walk radius applied to both networks.
2. **Aggregate above the stop id.** Adjacent stop ids split a route set at
   corridor locations. Stop-level rows are an intermediate, not a finding.
3. **Report both radii where they disagree.** 400 m is the headline
   quarter-mile access distance, 150 m the strict same-corner test; where they
   part company, the plan is consolidating stops.
4. **State the vintage.** Stop boardings are May 2025 and boardings-only; route
   ridership is April 2026; the current network is a summer 2026 feed and the
   proposed one is dated 2027.
